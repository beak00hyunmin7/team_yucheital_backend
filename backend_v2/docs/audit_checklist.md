# 백엔드 v2 점검 목록

기준 커밋: `6756e3ace2bf847d1228d496f5a702b2b7ec42af`

| 분야 | 점검 항목 | v1 결과 | v2 조치 | 상태 |
|---|---|---|---|---|
| 제품 흐름 | 업로드·예측·배수구 시나리오 분리 | 하나의 analyze API | 3개 API로 분리 | 완료 |
| 배수구 | 백엔드 자체 위치 탐색 여부 | D8/점수 후처리로 탐색 | 자동 탐색 미포함, 사용자 좌표만 저장 | 완료 |
| AI 계약 | RGB·강우량·시간·FiLM 지원 | 이미지 전용 구형 U-Net | RGB 256, rain, time, cond_dim=2 엄격 검사 | 완료 |
| AI 장애 | 모델 실패가 노출되는지 | D8 자동 폴백 | readiness/예측 503, fallback=false | 완료 |
| 추론 횟수 | 한 요청의 모델 호출 수 | 최대 6회 | 캐시 미스당 1회 | 완료 |
| 전처리 | AI 입력 재사용 가능 여부 | 요청마다 재생성 | 모델 입력 PNG와 고도 NPZ 저장·캐시 | 완료 |
| 입력 품질 | 저대비·형식 검사 | 기본 대비 검사 | 입력 유형·품질 보고서·가이드 추가 | 부분 완료 |
| 일반 등고선 | 선 고도 복원 | 미지원 | contour 보존 모드만 제공, heightmap 복원은 후속 | 후속 필요 |
| 저장 | 원본·전처리·결과 분리 | 전처리 미저장 | 종류별 별도 파일과 해시 저장 | 완료 |
| CSV | 이미지와 데이터 분리 | 기능 없음 | DB 원본, CSV 요청 시 생성 | 완료 |
| DB | 업로드·예측·시나리오 관계 | analysis_case 중심 | v2 전용 정규화 테이블 | 완료 |
| 마이그레이션 | 스키마 변경 이력 | create_all 중심 | Alembic 초기 마이그레이션 | 완료 |
| 캐시 | 모델 교체 시 무효화 | 문자열 버전 의존 | 체크포인트 SHA-256 포함 | 완료 |
| 중복 요청 | 같은 계산 동시 실행 | 가능 | unique cache key + 프로세스 single-flight | 부분 완료 |
| 다중 워커 | 분산 single-flight | 없음 | DB unique로 정합성 보호, 분산 락은 후속 | 후속 필요 |
| 이미지 응답 | 고해상도 overlay 재생성 | 서버에서 원본 크기 합성 | 256 맵 제공, 배수구는 프론트 레이어 | 완료 |
| 파일 쓰기 | 손상 가능성 | 직접 write | 임시 파일 후 atomic replace | 완료 |
| 모델 로딩 | 첫 요청 지연 | 기동 시 로드하나 실패 은닉 | 시작 시 로드·워밍업·상태 노출 | 완료 |
| GPU 동시성 | OOM 방지 | 제한 없음 | bounded semaphore, 기본 동시 추론 1 | 완료 |
| 로그 | 단계별 지연·캐시 | 전체 시간 위주 | processing_runs에 단계·지연·캐시·오류 | 완료 |
| 헬스체크 | DB·AI·스토리지 구분 | DB만 확인 | live/ready 분리 | 완료 |
| 보안 | CORS·암호 | 전체 CORS·기본 암호 | CORS allowlist·필수 DB 암호 | 부분 완료 |
| 인증/인가 | 사용자별 결과 보호 | 없음 | 별도 인증 계층 필요 | 후속 필요 |
| 백업 | DB·파일 일관 백업 | 없음 | 저장 구조만 분리, 운영 백업 작업 필요 | 후속 필요 |
| 테스트 | 실제 구조·캐시·사용자 배수구 | D8 중심 | 전처리·API·캐시·시나리오 테스트 | 완료 |
| 실제 모델 시험 | 체크포인트 로드·워밍업 | 보장 없음 | 검증 스크립트 제공 | 배포 환경 실행 필요 |

## 배포 전 필수 확인

- `python -m scripts.verify_checkpoint`가 제공된 `best.pt`로 성공하는지 확인
- readiness의 `checkpoint_hash`, `condition_dimension=2`, `fallback=false` 확인
- CPU/GPU 각각의 p50·p95 추론 시간 측정
- MySQL 마이그레이션과 downgrade를 복제 DB에서 확인
- DB와 파일 볼륨의 동시 백업·복원 시험
- 인증, 파일 접근권한, rate limit 연결
- 실제 등고선/DEM 고정 평가셋으로 모델 유효성 확인
